version: '3.8'

services:
  perfetto-sql-tools:
    build: .
    image: perfetto-sql-tools:latest
    container_name: perfetto-sql-tools
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
    restart: unless-stopped